# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "4.0"
metadata:
  name: employeedatamaster
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          destination: MTSL_F4D
          url: http://mgcdlapp001:8000
          client: '100'
        app:
          name: Z_EMP_DATA_SF
          description: Fiori activation SF Validation
          package: ZAFIORI
          transport: M4DK924905
        exclude:
          - /test/
